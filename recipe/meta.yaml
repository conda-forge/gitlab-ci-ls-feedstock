{% set name = "gitlab-ci-ls" %}
{% set version = "0.21.1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/alesbrelih/{{ name }}/archive/{{ version }}.tar.gz
  sha256: fb8d870eda127bcad325dba3e11227b353d4374fdc853fc2ca8a664546edda39

build:
  number: 0

requirements:
  build:
    - {{ compiler('rust') }}
    - {{ compiler('c') }}
    - {{ stdlib('c') }}
    - cargo-bundle-licenses
    - pkg-config
  host:
    - openssl
  run:
    - openssl

test:
  commands:
    - {{ name }} --help

about:
  home: https://github.com/alesbrelih/{{ name }}
  license: MIT
  license_file:
    - vscode-extension/LICENSE.txt
    - THIRDPARTY.yml
  summary: Language server for GitLab CI
  dev_url: https://github.com/alesbrelih/{{ name }}

extra:
  recipe-maintainers:
    - danielnachun
